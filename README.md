# Colors
Multi languaje library for easy scape sequency color coding
More languajes added in the future

# Usage

## Python
```python
def colortext(text : str, foregorund, background, style) -> str:

def colorprint(text : str, foregorund, background, style) -> None:

#auto completion should show you the types, if not just check the source code.

# color print is just a wrapper of colortext.

```